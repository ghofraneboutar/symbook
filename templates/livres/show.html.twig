<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

{% block body %}
    <div class="card">
        <div class="row no-gutters align-items-center">
            <div class="col-md-4">
                <img src="{{ livre.image }} " class="card-img" style="height: 300px; width: 300px;" />
            </div>
            <div class="col-md-8">
                <div class="card-body ">
                <h5 class="fw-bold">Titre :{{ livre.titre }}</h5>
                <p class="fw-bold">Resume:  {{ livre.resume }}</p>
                <h5 class="fw-bold">Éditeur: {{ livre.editeur }}</h5>
                <h5 class="fw-bold">Prix: {{ livre.prix }} </h5>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
